class Solution{
public:	
	void immediateSmaller(int *arr, int n) {
	    int i;
	    for(i=0;i<n-1;i++){
    	    if(arr[i]>arr[i+1])arr[i]=arr[i+1];
    	    else arr[i]=-1;
	    }
	    arr[i]=-1;
	}
};
